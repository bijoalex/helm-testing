# Default values for ibm-operand-config.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

metadata:
  name: ""
  namespace: ""
  annotations: {}
  labels: {}
spec:
  services:
    - name: ""
      spec: {}
