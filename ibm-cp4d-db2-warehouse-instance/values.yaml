# Default values for ibm-cp4d-db2-warehouse-instance.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

metadata:
  name: db2wh-cr

spec:
  license:
    accept: "true"
    license: Enterprise
